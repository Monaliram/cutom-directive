import { ChangeMeDirective } from './change-me.directive';

describe('ChangeMeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeMeDirective();
    expect(directive).toBeTruthy();
  });
});
